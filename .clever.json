{
  "deploy": {
    "webroot": "/public"
  },
  "hooks": {
    "postDeploy": "chmod -R 775 storage bootstrap/cache && php artisan migrate --force && php artisan config:cache && php artisan route:cache"
  }
}
